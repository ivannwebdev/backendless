import React from 'react'
import loader from '../images/Teak-spinner.svg'
import './Loader.css'

const Loader = () => (
  <div className='loader'>
    <img alt='' src={loader} />
  </div>
)

export default Loader
